/** Usable writing area inside an 11in page with 1in top / 1.25in bottom padding. */
export const EDITOR_PAGE_CONTENT_HEIGHT_PX = Math.round(8.75 * 96);

/** Usable text column inside an 8.5in page with 1in side margins at 96dpi. */
export const EDITOR_PAGE_CONTENT_WIDTH_PX = Math.round(6.5 * 96);
